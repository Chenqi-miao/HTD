# HTD 手部运动去噪 — 实验结果总结

**日期**：2026/8/10 – 2026/8/11
**任务**：3D 手部关键点去噪 + 运动学保真 + MANO 参数输出（InterHand，21 关节）
**最终管线**：`含噪关键点 → exp1 去噪 → HTD-Refine(s31 动力学修正) → MANO 拟合`

---

## 一、网络结构探索（结论：全部负结果，但明确了方向）

### 1.1 各实验负结果

| 实验 | 配置 | 结果 | 失败原因 |
|---|---|---|---|
| **Hybrid s15** | LRUTransformer+best（完全复刻 s31 赢家）| **10.00** vs 原版 s15 **9.75** | s31 赢家配置不迁移到 s15 |
| **Tiponly s15** | 只修 5 指尖，逐指尖 RoPE 时序，无空间注意力 | 指尖 **18.38**（仅比输入 19.12 好 3.8%）| 孤立时序无空间上下文，修不动指尖位置 |
| **Cascade v1/v2/v3** | 食指+腕 5 关节，沿运动链 空间+时序 交替级联 | ep0 慢热，停 | 单指链空间信息太少 |
| **exp1idx** | exp1 DSTFormer 直接跑食指 5 关节 | ep0 慢热，停 | 5 关节拿掉手掌/跨指上下文 |

### 1.2 核心教训

**"指头少≠更好学"**——去噪的难度在歧义，不在输出数量：
- 单关节含噪轨迹本身歧义，需要**空间线索**（手掌参照系 + 跨指协同 + 指长约束）消歧
- 隔离成单指/单指尖 = 拿掉解决问题的关键工具（空间上下文）= 任务变难
- 21 关节 exp1：ep0 好 30%；5 关节 exp1：ep0 差 8%——同架构唯一变量是关节数

---

## 二、关节级修复度分析（s15 完整模型，前 2000 窗）

| 分组 | input MPJPE | model MPJPE | 改善 |
|---|---|---|---|
| 腕 | 5.83 | 6.64 | **-13.8%**（恶化）|
| MCP（指根）| 8.62 | 5.07 | **+41.2%** |
| PIP | 10.93 | 6.86 | +37.3% |
| DIP | 13.33 | 9.52 | +28.6% |
| TIP（指尖）| 17.69 | 14.21 | **+19.7%** |

**关键发现**：
1. **修复梯度沿手递减**：MCP(41%) > PIP(37%) > DIP(29%) > TIP(20%)
2. **输入误差与修复能力相反**：指尖输入误差最大（17-19mm）但修复率最低
3. **腕被恶化（-13.8%）**：与掌心归一化（参考点落在腕附近）相关
4. 指尖是"输入最差 + 修复最弱"的**叠加重灾区**

---

## 三、运动学分析：exp1 vs exp2 vs s31（同数据同脚本，2000 窗）

| 指标 | **exp1** (DSTFormer) | **exp2 s15** (8层RoPE) | **s31** (LRU+混合) |
|---|---|---|---|
| corr_v | 0.938 | **0.966** | 0.951 |
| **corr_a** | 0.633 | **0.841** | 0.746 |
| corr_j | 0.408 | **0.690** | 0.545 |
| ratio_v | 0.936 | 0.936 | 0.933 |
| **ratio_a** | **0.889** | 0.851 | **0.925** |
| **ratio_j** | 0.864 | 0.765 | **1.008**（近乎完美）|

**分工结论**：
- **exp2/s31 强在高频结构跟随**（corr 全面高）：8 层 RoPE 深时序的优势
- **exp1 强在幅度保留**（ratio 更接近 1，尤其 ratio_j 0.864）
- exp1 位置更好（9.37 vs 9.75）但加速度相关性弱——**位置/幅度 vs 高频结构，分工相反**

---

## 四、HTD-Refine：用 s31 动力学精修 exp1（核心成果 ✅）

### 4.1 思路
exp1 弱点 = corr_a 低（高频结构没跟住）；s31 强项 = 动力学。用 s31 的动力学精修 exp1 的位置，让 exp1 位置 + s31 动力学的优点结合。

### 4.2 关键实现细节（三个必须）
1. **s31 必须用原生 31 帧输入**（动力学最好）——喂 15 帧会退化（acc 头 corr 0.57 vs 0.75）
2. **从头对齐**：exp1 吃 31 帧窗的前 15 帧，s31 动力学取前 15 帧
3. **参考 = s31 位置输出的差分**（不是 vel/acc 头——头是辅助的，不准）

### 4.3 能量函数
```
E = 1.0·‖v(p)−v_s31‖² + 10·‖a(p)−a_s31‖² + 20·‖p−p_exp1‖²   (无 jerk 项)
```
- `v_s31/a_s31`：s31 位置输出的速度/加速度差分（中心无关）
- 正则项 `‖p−p_exp1‖²` 把位置锚在 exp1 附近（保住位置/幅度）
- **jerk 项有害**（过度平滑，压幅度）

### 4.4 本地结果（2000 窗，reg20）

| 指标 | exp1 | 精修后 | 变化 |
|---|---|---|---|
| **corr_a** | 0.545 | **0.684** | **+0.139** |
| **corr_j** | 0.304 | **0.446** | **+0.142** |
| corr_v | 0.923 | **0.944** | +0.021 |
| MPJPE | 0.0263 | 0.0263 | **0 损耗** |

### 4.5 服务器全量结果（142,930 窗，正确口径 mask）

| 指标 | exp1 | 精修后 | 变化 |
|---|---|---|---|
| MPJPE (中间帧) | 10.55 mm | 10.60 mm | +0.05（几乎无损）|
| **corr_a** | 0.495 | **0.587** | **+0.092** |
| **corr_j** | 0.636 | **0.752** | **+0.116** |
| corr_v | 0.647 | 0.635 | -0.012 ≈ |
| ratio_j | 0.772 | **0.881** | 更接近 1 ✓ |

> ⚠️ **口径教训**：无 mask 全池化会把 corr 虚高到 0.97（被整体趋势主导），掩盖真实差异；必须用 **mask 有效点 + 全帧窗** 才能看到真实动力学（exp1 corr_a 0.50 vs 0.97）。

---

## 五、MANO 参数输出（✅ 优化式拟合，3.24mm）

### 5.1 数据约束
- npz_30 **无 GT MANO 参数**、**无图像数据** → 不能用 HaMeR 式图像回归 / 无法学习式监督
- **只能用优化式拟合**：拟合 MANO 参数使 21 关键点匹配去噪结果

### 5.2 基础设施
- 服务器：`manotorch 0.0.2` + `pytorch3d 0.7.9` + `MANO_RIGHT/LEFT.pkl`
- MANO 参数：`pose[48]`（root 3 + 15 关节 45）+ `shape[10]` + `trans[3]`（mm）
- 输出：21 关节 [B,21,3] + 778 顶点

### 5.3 拟合改进（HTD-Refine 论文思路）

| 方法 | 拟合误差 | vs GT 中间帧 | corr_a | corr_j |
|---|---|---|---|---|
| 逐帧独立（300 iters 全量）| 17.87 mm | 22.60 mm | 0.571 | 0.710 |
| 逐帧独立（800 iters 抽样）| 4.15 mm | 8.03 mm | 0.701 | 0.468 |
| **整窗优化（warm-start, lk=30）** | **3.24 mm** | **7.17 mm** | **0.749** | **0.532** |

**整窗优化（HTD-Refine 思路）**：
- 不再逐帧独立拟合，而是**整窗一起优化** MANO 参数（`pose[F,48] + shape[10] + trans[F,3]`）
- 能量 = `30·位置 + 1·速度一致性 + 1·加速度一致性 + 0.1·jerk + 0.01·正则`（warm-start 自逐帧结果）
- 时间一致性帮助消歧义 → **拟合更好 + 动力学更好**（corr_a/j 全升，ratio_j 从 1.07 修正到 0.95）

---

## 六、完整管线最终验证

```
含噪输入(18.82mm)
    ↓ exp1 去噪
去噪关键点(10.55mm)
    ↓ HTD-Refine (s31 31帧动力学)
精修关键点(10.60mm, corr_a +0.09, corr_j +0.12)
    ↓ MANO 整窗优化拟合
MANO 参数 pose/shape/trans (拟合 3.24mm, vs GT 7.17mm)
```

**全部在服务器端跑通**，142,930 窗全量。

---

## 七、关键结论汇总

1. **单指/隔离路线全失败**：空间上下文（整只手）是修复关键，指头少≠更好学
2. **s31 赢家不迁移 s15**：LRU 状态记忆优势只在长窗（31帧）兑现
3. **s31 动力学窗口依赖**：31 帧输入才有优势，15 帧退化——精修必须保留 31 帧上下文
4. **vel/acc 头不如位置输出差分**：辅助头训练不足，做参考要用位置输出
5. **HTD-Refine 有效**：corr_a/j 各 +0.1 左右，MPJPE 零损耗（正确口径下可见）
6. **MANO 整窗优化拟合有效**：3.24mm，时间一致性提升拟合和动力学
7. **口径必须用 mask**：无 mask 池化虚高 corr 掩盖真实差异

---

## 八、脚本清单（experiments/ 下）

| 脚本 | 用途 |
|---|---|
| `_server_pipeline_full.py` | 服务器全量管线（exp1→HTD-Refine→MANO）|
| `_mano_refine.py` | MANO 整窗优化拟合（HTD-Refine 思路）|
| `_htdrefine_s31.py` | 本地 HTD-Refine 验证 |
| `_fit_mano.py` | MANO 基础拟合验证 |
| `_per_joint_fix.py` | 关节级修复度分析 |
| `analyze_motion_fidelity.py` | 运动学保真分析（正确口径）|

---

## 九、下一步

- [ ] MANO 拟合扩到更多窗/全量（当前抽样 60-100 窗）
- [ ] 把整窗 MANO 优化接到完整管线（当前分开跑）
- [ ] 探索 DecoupledNet 时空解耦网络方向（修网络）
- [ ] 整理进正式方法/周报
