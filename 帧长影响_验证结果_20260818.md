# 输入帧长度影响 — 4 个验证实验结果 (2026-08-18)

> 全部基于服务器 `experiments/tools/analysis/` 下的脚本，模型 = exp2 纯 TemporalTransformer
> （s8/s15 用 `checkpoint/best.pth`，s31 用 `checkpoint/best_orig_full.pth` 原版 12.77，非 LRU 混合版）。
> 测试子集：前 3000 窗。数据文件：`experiments/eval_data/frame_len_*.json`。

---

## 一句话结论

**理论预测部分被推翻，修正后的故事更自洽：**

| # | 实验 | 原假设 | 结果 |
|---|---|---|---|
| 1 | 量 n_eff | 长窗核更平、有效帧饱和 | ✅ **证实** |
| 2 | 噪声扫描 | 噪声↑ 最优窗右移 (F*∝σ^{2/5}) | ❌ 拒绝；短窗任何噪声下都赢 |
| 3 | 误差频谱 | 长窗低通吃高频、高频修正为负 | ❌ 拒绝；s31 问题在**低频/DC** |
| 4 | bias∝aF² | 误差 ∝ 加速度 × F² | ◑ 部分：∝a 成立，∝F² 不成立 |

核心新认知：**模型只用 ~5-7 个有效帧，不管窗口是 8 还是 31。多出来的帧基本是分母。**
长窗的劣势不是"高频被抹平"，而是**低频/DC 误差没修干净**（注意力对整体平移不敏感）。

---

## 实验 1：n_eff（有效参与帧数）— ✅ 证实"有效帧饱和"

定义：`n_eff = 1/Σ_s w²`（每行权重越平，n_eff 越大，∈[1,F]）。模型实际"参考"多少帧。

| exp | F | 各层 n_eff (0→7) | 平均 n_eff | util_frac (n_eff/F) | 平均注意力距离 |
|---|---|---|---|---|---|
| s8 | 8 | 2.23→4.90 | **3.52** | **0.44** | 1.79 |
| s15 | 15 | 3.36→7.82 | **5.20** | **0.35** | 2.48 |
| s31 | 31 | 2.41→11.17 | **7.39** | **0.24** | 3.40 |

- 窗口 8→31（4 倍），有效帧只 3.5→7.4（2.1 倍），**util_frac 掉一半**。
- 平均注意力距离只从 1.8 涨到 3.4 帧 —— 有效感受野远小于窗口。
- 深层注意力更"散"（层 7 n_eff 最高），但即使最散的层在 s31 也只有 11/31。
- **结论：长窗多出来的帧模型基本不用 → "短窗好"是模型本质，不是训练没训好。**

---

## 实验 2：噪声扫描 — ❌ 拒绝 F*∝σ^{2/5}；✅ 短窗对任何噪声都鲁棒

模型 MPJPE（`*` = 该行最优）：

| 噪声 | s8 | s15 | s31 | 最优 |
|---|---|---|---|---|
| gauss-0 | **7.45** | 8.49 | 9.23 | s8 |
| gauss-5 | **8.21** | 9.18 | 9.64 | s8 |
| gauss-10 | **10.92** | 11.69 | 12.12 | s8 |
| gauss-20 | **18.29** | 18.63 | 20.57 | s8 |
| spike-1x30 | **8.74** | 9.54 | 10.78 | s8 |
| spike-2x30 | **9.68** | 10.26 | 11.66 | s8 |
| spike-1x60 | **9.08** | 10.65 | 14.91 | s8 |

- **高斯噪声**：σ=0~20mm，最优窗始终是 s8，不随 σ 右移 → `F*∝σ^{2/5}` 在这些固定模型上不成立。
- **尖峰噪声**：s8 的**输入基线更差**（尖峰在短窗占比高，input 16.0 vs s31 12.7），但模型输出仍然最好 → **短窗对离群帧结构性免疫**得到证实。
- 长窗的"多平均降噪"优势在输入侧存在（尖峰下 s31 input 更低），但**模型本身的精修能力主导**，短窗模型是更好的去噪器。
- 注：模型未经噪声微调，属 OOD 测试；但正因如此测的是"平滑特性本身"。

---

## 实验 3：误差频谱 — ❌ 拒绝"长窗吃高频"；s31 的弱点在低频/DC

误差功率分带（input_power / model_power / 修正率 `1−model/input`）：

| exp | 低频 [0,.1) | 中频 [.1,.25) | 高频 [.25,.5) |
|---|---|---|---|
| s8 | 292→155 (**47.0%**) | 11→8.7 (21.3%) | 4.1→2.1 (47.5%) |
| s15 | 698→247 (**64.7%**) | 22→8.3 (62.2%) | 12→2.2 (**81.8%**) |
| s31 | 690→373 (**45.9%**) | 15→8.0 (46.6%) | 6.1→1.2 (**80.6%**) |

- **没有任何模型高频修正为负** → "长窗把高频真信号（指尖尖峰）抹平"不成立。
- 误差功率 90%+ 集中在**低频**（input 690 vs 高频 6）。
- s31 的短板是**低频修正最差（45.9%）**：低频功率剩 373，远高于 s15 的 247。
- 高频（>0.25 cyc/frame）三模型都修得很好（80%+）。
- **结论：s31 位置差是"低频/DC 漂移没修干净"，不是高频损伤。** 这与 DC 偏移实验（注意力对整体平移不敏感）一致，也解释了为什么 LRU（携带状态/摘要）在 s31 有效。

---

## 实验 4：bias∝a·F² — ◑ ∝a 成立，∝F² 不成立

按 GT 加速度分 6 桶，内圈帧逐帧·关节统计：

| bucket | mean_a | s8 | s15 | s31 |
|---|---|---|---|---|
| 0 | 0.17 | 4.79 | 5.73 | 6.79 |
| 1 | 0.47 | 5.90 | 6.53 | 7.30 |
| 2 | 0.94 | 7.09 | 7.84 | 8.54 |
| 3 | 2.03 | 7.58 | 8.82 | 9.56 |
| 4 | 4.50 | 8.06 | 9.47 | 10.31 |
| 5 | 11.30 | 9.90 | 11.39 | 12.48 |

线性拟合 `MPJPE ≈ c0 + c1·a`：

| exp | F | c1 | c0 | R² |
|---|---|---|---|---|
| s8 | 8 | 0.372 | 6.01 | 0.80 |
| s15 | 15 | 0.438 | 6.88 | 0.82 |
| s31 | 31 | 0.460 | 7.67 | 0.87 |

- **误差随加速度增长：成立**（三个模型斜率都 >0，R²≈0.8-0.87）。
- **但斜率几乎不随 F 放大**：s31/s8 斜率比 = 1.24，而理论 F² 比 = 15 → `∝F²` 不成立。
- F 的差别主要是**常数偏移 c0**（6.01→6.88→7.67），不是加速度相关部分。
- **自洽解释**：偏差公式里的"窗宽"是**有效核宽 μ₂**，不是原始 F。实验 1 已证 n_eff 饱和（~7），所以有效核宽在各 F 下差不多 → 斜率自然不随 F² 涨。

---

## 修正后的结论（替代原文档第 2 节的三个假说）

1. **"长窗低通吃高频 → 崩盘"是错的**。频谱显示高频修正率三模型都很高，s31 的弱点在低频/DC。
2. **"长窗噪声暴露多 → 该用短窗"方向对，但机制不是 F² bias**。噪声扫描显示短窗对高斯和尖峰都鲁棒，而 bias 不随 F² 放大。
3. **真正的统一解释**：注意力有效感受野饱和（~5-7 帧）。窗口再长，模型也不多用一个有效帧 → 位置误差不被 F 改善；长窗唯一的额外劣势是注意力对 DC 平移不敏感 → 低频漂移修不掉，残留误差更大。
4. **对架构的启示**：长窗需要的不是"更深"（深度不改变有效核宽），而是"低频/DC 的显式校正"（你已试过 DC-offset，单独无效）或"状态压缩让模型用到全窗"（LRU 在 s31 赢，正因此）。

---

## 产物

- 脚本：`experiments/common/frame_len.py`（共享加载/预测/注意力累计）
  `experiments/tools/analysis/analyze_neff.py` / `analyze_noise_sweep.py` / `analyze_error_spectrum.py` / `analyze_bias_aF2.py`
- 数据：`experiments/eval_data/frame_len_neff.json` / `frame_len_noise_sweep.json` / `frame_len_error_spectrum.json` / `frame_len_bias_aF2.json`
- 复跑：
  ```bash
  CUDA_VISIBLE_DEVICES=3 python tools/analysis/analyze_neff.py --n-test 3000
  CUDA_VISIBLE_DEVICES=3 python tools/analysis/analyze_noise_sweep.py --n-test 3000
  CUDA_VISIBLE_DEVICES=3 python tools/analysis/analyze_error_spectrum.py --n-test 3000
  CUDA_VISIBLE_DEVICES=3 python tools/analysis/analyze_bias_aF2.py --n-test 3000
  ```
